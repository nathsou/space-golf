{
  "name": "space-golf",
  "version": "0.0.2",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build-gh": "tsc && vite build --base=/space-golf/",
    "serve": "vite preview"
  },
  "dependencies": {
    "parsecs": "^0.1.3"
  },
  "devDependencies": {
    "typescript": "^4.3.2",
    "vite": "^2.6.0"
  }
}